# Devbox — reproducible dev environment

Единый Docker-образ для разработки на **Python + TypeScript/React + Flutter + C++** с (zsh + oh-my-zsh + Powerlevel10k).

---

## ✨ Что внутри
- **Python 3 + uv**
- **Node.js 20 + pnpm**
- **Flutter (без Android SDK, для сборок/тестов)**
- **Java 17 (для Android-сборок)**
- **C++ toolchain (g++, clang-15, cmake, valgrind, cppcheck)**
- **zsh + oh-my-zsh + Powerlevel10k + плагины**
- ssh-agent passthrough
- git config passthrough (для коммитов/пушей из контейнера)
- поддержка **docker-in-docker** и **docker-outside-of-docker**

---

## 🚀 Установка и сборка

Собрать образ локально:
```bash
docker build -t devbox:latest .
```
## Запуск
```bash
#Windows
.\dev.ps1 dev      # запустить контейнер в папке проекта
.\dev.ps1 dev-dood # то же самое, но с доступом к docker демону хоста
.\dev.ps1 dind     # поднять изолированный docker-daemon (DinD)

#linux и macos
make dev           # запустить контейнер в папке проекта
make dev-dood      # то же самое, но с доступом к docker демону хоста
make dind          # поднять изолированный docker-daemon (DinD)
```

---

## 🏆 Competitive Programming

### Генерация контеста
```bash
./contest-gen.sh 1920 5    # создаст контест 1920 с 5 задачами (A-E)
```

Структура создаваемых файлов:
```
contests/
  1920/
    1920A.cpp
    1920B.cpp
    1920C.cpp
    1920D.cpp
    1920E.cpp
    run.sh
```

### Компиляция и запуск решения
```bash
cd contests/1920
./run.sh 1920A    # скомпилирует и запустит решение для задачи A
```

**Примечание**: Все файлы в папке `contests/` игнорируются git'ом, так что вы можете свободно решать задачи не засоряя репозиторий.